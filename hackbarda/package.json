{
  "name": "hackbarda",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix kasukabe-store\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix kasukabe-store"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
